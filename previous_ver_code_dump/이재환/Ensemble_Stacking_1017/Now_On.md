## 딥러닝/머신러닝 사용 모델
SAINT

## LLM 사용 모델
Gemini 2.5 flash api

## To-do List
1. 미리 깃허브에서 다운로드한 ./SAINT/ 폴더 내의 SAINT 딥러닝 모델 py 파일과 관련 py 파일들을 현재 내 프로젝트에 맞게 수정.
2. 학습 결과를 시스템/유저 프롬프팅 한 LLM api에 전달해서 결과를 반환하도록 코딩


## Check List
- 각 모델별 프로토타입을 만들고 학습을 마치면 plot(acc, loss)으로 학습 추이를 확인.
- 또한 모델의 validation 에 대해 여러 metrics를 통해 성능 측정하는 부분 있어야 하고, 측정 결과를 print 하고 plot 으로 result 를 알 수 있도록 해야 함.
- verbose 는 항상 1로 켜둠
- 하이퍼파라미터, api key, LLM의 델타값, metrics를 이미지로 저장만 할건지 팝업으로도 띄울건지에 대한 토글은 config 파일에서 직접 설정할 수 있도록 코딩해야 함.
- 추후 성능 향상을 위해 input data에 axis=1 로 concat 될 수 있으니, input 사이즈를 동적으로(가변) 받을 수 있도록 해야함.(만약 모델이 사이즈 지정이 필요없다면 설정x)
- 모델의 결과를 LLM(gemini 2.5 flash api)을 이용하여 답변을 생성해야 하므로 feature importance 나 예측값 혹은 클래스별 probability를 전달할 수 있도록 해야함.
- 전달할 값의 출처 컬럼이나 예측 타겟은 [~~,~~,~~,~~] 으로 함.
- LLM 과의 대화는 streamlit 화면에서 진행할 수 있도록 하고 LLM의 시스템 프롬프트와 유저 프롬프트를 직접 설정할 수 있도록 함.
- 최대한 모듈화 해서 각 기능의 디버깅이나 유지보수를 용이하게 할 수 있도록 하나, 과도하게 기능을 쪼개지는 않도록 함. 
- 데이터는 ./dataset/ 폴더 안에 있음.
- 라인마다의 주석은 포함하지 않음.(과도한 lineby 주석을 달지 말라는 것임.)